# ME_LessonPlanGen — Lesson plan generator

Generate lesson plans from inputs (subject, topic, level, duration, etc.). Produces structured lesson-plan documents for teachers.

## Commands

```bash
TBD      # dev — to be decided after brainstorming
TBD      # test — to be decided after brainstorming
TBD      # build — to be decided after brainstorming
```

## Project Structure

```
src/               # source code (TBD)
tests/             # test suite (TBD)
docs/              # documentation
```

## Architecture

TBD — pending requirements gathering and design.

See @docs/开发类/方案架构设计过程.md for the live design record.

## Conventions

TBD — to be decided.

## Boundaries

- Do NOT commit `.env` / `credentials.*`
- Do NOT edit files in `00_模板/` (root template, frozen)

## Reference Documents

- Status: @memory/下次会话.md — current state / blocked / next steps
- Architecture: @docs/开发类/方案架构设计过程.md
- Project rules: @.claude/rules/ — inherited from root
- Global: `~/.claude/CLAUDE.md`
